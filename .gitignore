node_modules/
dist/
build/
*.log

# Telemetry data files (large, user-specific)
data/*.duckdb

# Track/car hero photos, car in-game photos, and manufacturer badges,
# uploaded via the admin panel — runtime content, not committed (same
# reasoning as the .duckdb files above)
data/track-photos/
data/car-photos/
data/manufacturer-photos/

# Env
.env
.env.local

# Claude Code local settings (machine-specific permissions)
.claude/
